services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: pcparts
      POSTGRES_USER: pcparts
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change-me}
    ports:
      - "127.0.0.1:5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pcparts -d pcparts"]
      interval: 5s
      timeout: 3s
      retries: 10
volumes:
  pgdata:
